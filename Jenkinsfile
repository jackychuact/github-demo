pipeline {
    agent any

    stages {
        stage('Verify') {
            steps {
                // This command lists the files to verify they were checked out correctly 2nd times
                bat 'dir'
            }
        }
    }
}
