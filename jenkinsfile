pipeline {
    agent any

    stages {
        stage('Verify') {
            steps {
                // This command lists the files to verify they were checked out correctly update for 3rd times
                bat 'dir'
            }
        }
    }
}
